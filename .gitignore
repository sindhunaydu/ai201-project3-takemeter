# Intermediate working files behind the canonical labeled CSV.
# raw_comments.json contains Reddit usernames; candidates.json/labels.json are the
# filtered pool + raw annotations. The committed, username-scrubbed artifact is
# data/takemeter_dataset.csv.
data/raw_comments.json
data/candidates.json
data/labels.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store

# Secrets (never commit API keys)
.env
*.key
